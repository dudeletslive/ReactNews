
'use strict';

import GlobalTree from '../global.tree';

describe('Global Tree', function() {

  it('provides the "Global Tree" instance', () => {
    // Expect it to exist
    expect(GlobalTree);
  });

});
